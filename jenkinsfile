pipeline {
    agent any
    stages {
        stage("checkout") {
            steps {
                checkout scm
            }
        }
        stage ("Deployment") {
            steps{
                sh "sudo su cp -r /var/lib/jenkins/workspace/new-web-app/ /var/www/html/"
            }
                
        }
    }
}
