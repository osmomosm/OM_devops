pipeline {
    agent any
    stages {
        stage("checkout") {
            steps {
                checkout scm
            }
        }
        stage ("Deployment") {
            steps{
               sudo -S cp -r /var/lib/jenkins/workspace/new-web-app/ /var/www/html/
            }
                
        }
    }
}
